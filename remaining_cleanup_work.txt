Laki Web IDE - Remaining Android-to-Web Cleanup Task List

I have identified the following remaining tasks to fully transition the editor logic to a web-focused system while keeping your "Smart Covers" intact.

1. ViewPropertyItems.java (The Property Panel)
   - Method g(ViewBean): Still uses Android class checks (TextView/EditText). Needs to be updated to use .supportsText() and .supportsInput().
   - Method i(ViewBean) [Saving Logic]:
     * Remove 'property_weight_sum' case (Not needed for CSS Flexbox).
     * Remove 'property_lines' case (Web uses CSS for overflow/clamping).
     * Remove 'property_ime_option' (Android system keyboard only).
     * Remove 'property_single_line' switch case (Web uses white-space: nowrap).
     * Remove 'property_divider_height' check (Legacy Android ListView).
   - ID Renaming Block:
     * Optimization: Finalize the removal of loops that search for legacy Android adapter events (onBindCustomView) and MoreBlocks, keeping only standard web events (onClick, etc.).

2. LayoutBean.java (Layout Data Model)
   - Remove 'weightSum' field: Redundant for web layouts.
   - Keep 'weight', 'gravity', 'layoutGravity': These are your Smart Covers for CSS Flexbox properties.

3. TextBean.java (Text Data Model)
   - Remove 'imeOption', 'singleLine', 'line': These are purely Android-native behaviors.
   - Keep 'inputType': Used as a Smart Cover for HTML <input type="..."> tags.

4. General File Cleanup
   - ViewPane.java: Strip remaining unused imports (e.g., SeekBar, CalendarView, TabLayout) left over from deleted blocks.
   - ViewBean.java: Remove any leftover constants that were missed in the previous cleanup pass (e.g., CHOICE_MODE, SPINNER_MODE).

Status:
- Capability-based system (supportsText, supportsLayout) is implemented in ViewBean.
- Most UI property suppressions are active.
- ID format [filename][tag]_[count] is fully implemented and tested.

Ready to proceed with surgical cleanup of these specific lines.
